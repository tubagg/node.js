 export default (name) => {
    console.log(`hello ${name}`);
};

  const topla = (a,b) => a+b;
  const cikar = (a,b) => a-b;

  const text = 'Text';
  const user = {
    name:'Tugba',
    surname:'Bozkurt',

 };

  const users = [{
    name:'Ayşe',
    surname:'Yıldırım',
    
 },{
    name:'Emir',
    surname:'Seyaan',
 },
];

export{
    topla,
    cikar,
    text,
    user,
    users,
};


